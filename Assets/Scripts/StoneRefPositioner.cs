using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StoneRefPositioner : MonoBehaviour
{
    /*public Camera Cam = Camera.main;
    // Start is called before the first frame update
    void Start()
    {
       
    }

    // Update is called once per frame
    void Update()
    {
        transform.position = new Vector3(Cam.transform.position.x, Cam.transform.position.y - 0.7f, Cam.transform.position.z + 2);
    }*/
}
